배열(array)이란 각각의 요소(item) 데이터가 순서를 가지고 나열된 집합입니다.
이러한  배열은 대괄호([])로 둘러싸여 있습니다.	
배열은 쉼표(,)를 사용하여 여러 개의 데이터를 나열할 수 있습니다.
index를 사용하여 요소데이터에 접근할 수 있습니다.

object는 키와 값으로 구성되어있고 합쳐서 속성이라고 부릅니다.
프로퍼티의 값으로 함수가 올 수도 있는데, 이러한 프로퍼티를 메소드(method)라고 합니다.
key를 사용하여 key, value에 접근할 수 있습니다. 